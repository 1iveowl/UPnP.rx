# DECISIONS.md — the ledger

One line per settled decision, newest first, pointing at the full record. Documents stay
where they are; this index exists so no session re-litigates or rediscovers. Add a line
whenever a decision lands anywhere else.

| Date | Decision | Record |
|---|---|---|
| 2026-07-29 | 6.0.0 scope Q1-Q5 settled: the **SCPD source generator is in**, gated on a byte-identity proof that generated wrappers compose the same SOAP envelopes as the hand-written `InternetGateway` (green gate → `InternetGateway` adopts it; red gate → the generator still ships, adoption waits, reason recorded); 6.0.0 agreed; the port-mapping read-back honesty fix rides along (`PortMappingEntry.LeaseDuration`/ports become nullable, so a gateway's silence stops reading as "never expires"); the **raw wire log stays shelved** and remains upstream territory, as `Announcements()` already documents - cost measured first (~60 lines for `ParseFailures`, ~120 for raw capture) and it was never gated on 10.0.0, since all of it shipped in 9.1.0; UPNPRX003 stays at Warning | [upnp-rx-v6.0-plan.md §13](upnp-rx-v6.0-plan.md) |
| 2026-07-29 | Enforcement tooling: **an API change beats a rule, and an existing tool beats a new one.** Four of seven candidate analyzer rules were deleted before any were written - three because the API already made the state unrepresentable (`ushort` ports, `Protocol` enum, `TryService`), one because `BannedSymbols.txt` bans the scheduler-less Rx time operators per-overload (measured: RS0030 fires, the scheduler-taking overload does not). Time-model rule 3 is therefore repo-local enforcement, deliberately not a shipped analyzer - a consumer's own `Throttle` is not our mistake, and firing there would blow the zero-false-positive budget | [upnp-rx-v6.0-plan.md §3, §7a](upnp-rx-v6.0-plan.md) |
| 2026-07-27 | Clause 4.1.1 **shipped in 5.0.0**, not deferred: a subscription watches its device's presence and is abandoned on a byebye or an unannounced boot change, with no UNSUBSCRIBE on the void SID and recovery only when CONFIGID proves the `eventSubURL` has not moved. Presence comes from `Roster()` rather than a second boot tracker, so `Events()` now starts the roster engine | [upnp-rx-v5.0-plan.md §12](upnp-rx-v5.0-plan.md) |
| 2026-07-27 | A device's advertised lifetime is reported verbatim (`Announcement.MaxAge` is null for "none announced", `TimeSpan.Zero` for a genuine `max-age=0`), while the **roster** substitutes `RosterExpiryFallback` for any non-positive lifetime - a device that keeps announcing cannot have meant "expire me now", and honouring it literally would flap the card on every message. Enabled by SSDP.UPnP.PCL 9.1.0 (upstream candidate 6, now fully resolved) | [upnp-rx-project-plan.md §9](upnp-rx-project-plan.md) |
| 2026-07-27 | 5.0.0 scope Q1-Q5 settled: boot identity is a `BootSignature` value type (not two raw properties - the failure mode of comparing half an identity is silent); all four version witnesses modelled; presence types go to **`UPnP.Rx.Presence`** (the library already namespaces every subsystem, so the root-level `Announcement` was the anomaly - the earlier recommendation to flatten was withdrawn); 5.0.0 agreed; the disagreement badge shows by default but only when claims actually differ | [upnp-rx-v5.0-plan.md §7](upnp-rx-v5.0-plan.md) |
| 2026-07-26 | **Superseded by the row above (agreed):** the next release is **5.0.0**, not 4.3 - `DiscoveredDevice.BootId` must go `uint` → `uint?` (shipped API), which is breaking; going major also unblocks the namespace harmonization already gated on one. Theme: absence must be representable | [upnp-rx-v5.0-plan.md](upnp-rx-v5.0-plan.md) |
| 2026-07-26 | UDA 2.0 names **no authority** between `SERVER`'s `UPnP/x.y` token and the description's `<specVersion>` - both are "shall", the spec calls them "the same information", so a mismatch is device non-conformance, not format noise (`<specVersion>` is *not* pinned to 1.0 - verified against the document). Hence **4.3** shows one badge per source with provenance plus a disagreement flag, never a silently reconciled version. Blocked on upstream candidate 9 (`ParseDeviceInfo` misreads the version) | [upnp-rx-v4.1-plan.md §7e](upnp-rx-v4.1-plan.md); [upnp-rx-project-plan.md §9](upnp-rx-project-plan.md) |
| 2026-07-26 | UPnP 1.0 devices send no `BOOTID`, so reboot detection is blind for them: **4.3** gains the boot-signature work (`BootId` → nullable/`BootSignature`, `NLS` fallback resolved via the `OPT` prefix, dashboard label guard). Three upstream candidates recorded alongside: 6 `ParseMaxAge` drops valid compound `CACHE-CONTROL`, 7 absent-vs-zero `BOOTID`, 8 `NLS` unparsed | [upnp-rx-v4.1-plan.md §7d](upnp-rx-v4.1-plan.md); [upnp-rx-project-plan.md §9](upnp-rx-project-plan.md) |
| 2026-07-26 | 4.1.1 is skipped; the structural batch ships as **4.2.0** (API analyzer ledger, DescriptionCache extraction, IUpnpClient, DECISIONS.md, CI snupkg + trim smoke); previously "4.2" deferrals move to **4.3** (local-IPv4 helper promotion, LoggerMessage, Announcement/Roster namespace harmonization at a major) | this file; [upnp-rx-v4.1-plan.md §7c](upnp-rx-v4.1-plan.md) |
| 2026-07-26 | Dedup shape: engine skeleton → `EngineSource<T>`, timed HTTP → `TimedExchange`, test vocabulary → `TestKit`; per-item Rx pipeline shape is idiom, not duplication | [upnp-rx-v4.1-plan.md §7c](upnp-rx-v4.1-plan.md) |
| 2026-07-25 | SSDP activity log ships as the parsed-envelope timeline (`Announcements()`, passive, live-only); the raw wire log is upstream territory, deferred | [upnp-rx-v4.1-plan.md §4](upnp-rx-v4.1-plan.md) |
| 2026-07-25 | Activity retention: 20 rows/device + 1 h age bound, client-side constants; long retention = file logging, not this sample | [upnp-rx-v4.1-plan.md §7b](upnp-rx-v4.1-plan.md) |
| 2026-07-25 | 4.1 scope Q1-Q7: roster = union + replay (no DynamicData in the library); self-heal folds into the roster; action invocation confirms before mutating; `UPnP.Rx.Eventing.Av` + `SelectAvChanges()`; message log deferred; ReactiveUI stays; upstream filings deferred | [upnp-rx-v4.1-plan.md §5](upnp-rx-v4.1-plan.md) |
| 2026-07-25 | Locks: streams get Rx serialization operators, lifecycles get the smallest visible `Lock`, never both for one piece of state; `ControlPoint.Start` lock removed via upstream 8.0.0 | [upnp-rx-v4.0-code-review.md addendum](upnp-rx-v4.0-code-review.md) |
| 2026-07-25 | Permanent SUBSCRIBE refusals (404/405/410/501) end the stream with `SubscriptionRefused` + `OnError`; 412/5xx/timeouts keep retrying | [upnp-rx-v4.0-eventing-plan.md §4 Q6](upnp-rx-v4.0-eventing-plan.md) |
| 2026-07-25 | ReactiveUI lifecycle: ctor-composed pipelines + explicit disposal (no `WhenActivated`); settable VM properties source-generated, OAPHs/commands hand-written (WASM scheduler must be explicit) | [upnp-rx-project-plan.md §8.8](upnp-rx-project-plan.md) |
| 2026-07-24 | `SimpleHttpListener.Rx` becomes a direct dependency (7.2.0+, packet-info fix) | [upnp-rx-project-plan.md §8.7](upnp-rx-project-plan.md) |
| 2026-07-24 | Eventing Q1-Q5: observable-first `Events()`; auto-resubscribe surfaced as events; ephemeral callback port; LastChange helper deferred (shipped 4.1); late-subscriber replay included (hand-rolled ref-count over the gate) | [upnp-rx-v4.0-eventing-plan.md §4](upnp-rx-v4.0-eventing-plan.md) |
| 2026-07-24 | Foundation set: package name; first release is 3.0.0 (lineage); IGD leases auto-renew, abrupt dispose is safe by finite lease; raw discovery streams in v1 (roster later); `ParseResult<T>` copied, zero coupling; search target is the consumer's choice | [upnp-rx-project-plan.md §8.1-6](upnp-rx-project-plan.md) |
| standing | Time model (one `TimeProvider`, build-enforced), disposal model (graceful async / abrupt sync), 10 Rx rules, leniency policy, dependency lock | [upnp-rx-project-plan.md §5](upnp-rx-project-plan.md) |
| standing | Upstream gaps become issue candidates in plan §9, never fixed from this repo; candidates 3/4/5 resolved by SHL 7.2/7.3 + SSDP 8.0, 1/2 open | [upnp-rx-project-plan.md §9](upnp-rx-project-plan.md) |
